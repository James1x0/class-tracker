import {
  titleCase
} from '../../../helpers/title-case';
import { module, test } from 'qunit';

module('TitleCaseHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = titleCase(42);
  assert.ok(result);
});
